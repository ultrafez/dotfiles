[user]
	name = Alex Silcock
[core]
	editor = vim
	excludesfile = ~/dotfiles/gitignore_global
[color]
	ui = true
[push]
	default = current
[alias]
	co = checkout
	c = "!f() { git branch -a | grep -v remotes | grep $1 | xargs git checkout; }; f"
	ci = commit
	st = status
	s = status
	f = fetch
	ff = merge --ff-only
	b = branch
	r = rebase
	m = merge
	d = diff
	dc = diff --cached
	rh = reset --hard @{u}
	#review = "!f() { git merge $1 --no-commit --no-ff; git diff --cached; }; f"
[rerere]
	enabled = true
